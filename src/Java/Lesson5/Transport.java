package Java.Lesson5;

public class Transport {
    static int power;             // Объявляем переменную мощность (л\с)
    double maxSpeed;       // Объявляем переменную максимальное скорость (км\ч)
    double weight;         // Масса (кг)
    String brand;          // Объявляем переменную марка

}